(()=>{
    
    const enviarMision = (xmen:any) => {
        console.log(`Enviando a ${xmen.nombre} a la Mision`);
    }

    const wolverine = {
        nombre: 'Logan',
        edad: 30
    };

    enviarMision(wolverine);
    


})();